---
phase: 01-design-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - astro.config.mjs
  - tsconfig.json
  - package.json
  - netlify.toml
  - src/layouts/BaseLayout.astro
  - src/styles/global.css
  - src/styles/tokens.css
  - src/styles/typography.css
  - src/styles/layout.css
  - src/styles/animations.css
  - src/pages/index.astro
autonomous: true
requirements: [FOUN-01, FOUN-02, FOUN-03, FOUN-04, FOUN-05]

must_haves:
  truths:
    - "npm run dev starts a local Astro server on localhost:4321"
    - "npm run build produces a static dist/ folder with zero errors"
    - "CSS custom properties for all colors, spacing, typography, and grid are defined in tokens.css"
    - "Inter Variable and JetBrains Mono Variable load via Astro Font API with no CSS @import"
    - "Toggling data-theme=dark on html swaps all color tokens"
    - "Theme persists in localStorage across hard reloads"
    - "No flash of wrong theme on initial load or after View Transition navigation"
    - "@layer declaration order is the first rule in global.css"
  artifacts:
    - path: "astro.config.mjs"
      provides: "Astro 6 project config with Font API and static output"
      contains: "output: 'static'"
    - path: "src/styles/tokens.css"
      provides: "All CSS custom properties for both light and dark modes"
      contains: "[data-theme=\"dark\"]"
    - path: "src/styles/global.css"
      provides: "@layer declaration, reset, grain overlay styles"
      contains: "@layer reset, tokens, base, components, utilities"
    - path: "src/styles/typography.css"
      provides: "Type scale, font stacks, line heights"
      contains: "var(--font-inter)"
    - path: "src/styles/layout.css"
      provides: "12-col grid, breakpoint helpers, spacing utilities"
      contains: "--grid-cols"
    - path: "src/layouts/BaseLayout.astro"
      provides: "HTML shell with Font components, theme script, grain overlay div"
      contains: "is:inline"
  key_links:
    - from: "astro.config.mjs"
      to: "src/layouts/BaseLayout.astro"
      via: "Font API cssVariable names must match"
      pattern: "--font-inter"
    - from: "src/styles/tokens.css"
      to: "src/layouts/BaseLayout.astro"
      via: "data-theme attribute set by inline script, consumed by CSS"
      pattern: "data-theme"
    - from: "src/styles/global.css"
      to: "src/styles/tokens.css"
      via: "@layer tokens wraps all token declarations"
      pattern: "@layer tokens"
---

<objective>
Scaffold the Astro 6 project and build the complete CSS design system foundation: tokens, typography, layout grid, cascade layers, dark mode infrastructure with flash prevention, and the BaseLayout shell.

Purpose: Every subsequent phase imports from these files. Getting tokens, layers, fonts, and dark mode right here prevents revisiting fundamentals later.
Output: A buildable Astro project with complete CSS architecture, self-hosted variable fonts, and working dark mode toggle.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-design-system/01-CONTEXT.md
@.planning/phases/01-design-system/01-RESEARCH.md
@.planning/phases/01-design-system/01-VALIDATION.md

Canonical design reference (must read before implementing tokens):
@C:\Users\mzbir\Desktop\Brain\Personal\02 - Projects\Portfolio Website\Visual Direction and References.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Astro project and configure Font API</name>
  <files>
    astro.config.mjs
    tsconfig.json
    package.json
    netlify.toml
    src/pages/index.astro
  </files>
  <read_first>
    .planning/phases/01-design-system/01-RESEARCH.md (Pattern 3: Astro Font API Configuration, Standard Stack section, Environment Availability)
    .planning/phases/01-design-system/01-CONTEXT.md (D-01 through D-08 decisions)
  </read_first>
  <action>
    1. Create the Astro 6 project using `npm create astro@latest . -- --template minimal --no-install` (the directory already exists as a git repo, use current directory). If the CLI asks about TypeScript, select strict.

    2. Install dependencies:
       ```
       npm install
       npm install @fontsource-variable/inter @fontsource-variable/jetbrains-mono
       npm install astro-icon pixelarticons
       ```

    3. Configure `astro.config.mjs` with these exact settings:
       ```js
       import { defineConfig, fontProviders } from 'astro/config';

       export default defineConfig({
         output: 'static',
         site: 'https://adampalencar.netlify.app',
         fonts: [
           {
             provider: fontProviders.fontsource(),
             name: 'Inter Variable',
             cssVariable: '--font-inter',
             weights: ['100 900'],
             styles: ['normal'],
             fallbacks: ['system-ui', 'sans-serif'],
           },
           {
             provider: fontProviders.fontsource(),
             name: 'JetBrains Mono Variable',
             cssVariable: '--font-jetbrains-mono',
             weights: ['100 800'],
             styles: ['normal'],
             fallbacks: ['Courier New', 'monospace'],
           },
         ],
       });
       ```

    4. Set `tsconfig.json` to `strict` mode (Astro's minimal template may set this already; verify).

    5. Create `netlify.toml`:
       ```toml
       [build]
         command = "npm run build"
         publish = "dist"

       [[redirects]]
         from = "/*"
         to = "/404"
         status = 404
       ```

    6. Create a minimal `src/pages/index.astro` stub that imports BaseLayout (will be created in Task 2) with just a heading "Portfolio" so the dev server has a route. This is a placeholder; real homepage is Phase 4.

    7. Verify: `npm run build` completes with zero errors and produces a `dist/` directory.

    Per FOUN-01: static output, no @astrojs/netlify adapter.
    Per FOUN-03: fonts configured via Astro Font API, not CSS @import.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `astro.config.mjs` contains `output: 'static'`
    - `astro.config.mjs` contains `cssVariable: '--font-inter'`
    - `astro.config.mjs` contains `cssVariable: '--font-jetbrains-mono'`
    - `astro.config.mjs` contains `fontProviders.fontsource()`
    - `package.json` contains `@fontsource-variable/inter`
    - `package.json` contains `@fontsource-variable/jetbrains-mono`
    - `package.json` contains `astro-icon`
    - `package.json` contains `pixelarticons`
    - `netlify.toml` contains `publish = "dist"`
    - `tsconfig.json` exists
    - `dist/` directory exists after `npm run build`
    - No `@astrojs/netlify` in package.json
  </acceptance_criteria>
  <done>Astro 6 project builds successfully with Font API configured for both variable fonts. Netlify deploy config in place. Zero errors on build.</done>
</task>

<task type="auto">
  <name>Task 2: Create CSS architecture and BaseLayout with dark mode</name>
  <files>
    src/styles/global.css
    src/styles/tokens.css
    src/styles/typography.css
    src/styles/layout.css
    src/styles/animations.css
    src/layouts/BaseLayout.astro
    src/pages/index.astro
  </files>
  <read_first>
    .planning/phases/01-design-system/01-RESEARCH.md (Pattern 1: @layer, Pattern 2: Token File, Pattern 4: Dark Mode Flash Prevention, Pattern 5: Grain Overlay, BaseLayout.astro Shell code example, Dark Mode Palette section)
    C:\Users\mzbir\Desktop\Brain\Personal\02 - Projects\Portfolio Website\Visual Direction and References.md (CSS Custom Properties Starter section, Typography section, Grid section, Accent Color section)
    src/pages/index.astro (current stub from Task 1)
    astro.config.mjs (verify Font API cssVariable names for consistency)
  </read_first>
  <action>
    Create the complete CSS architecture. All files use @layer blocks matching the declaration order.

    **src/styles/global.css** (per D-06, FOUN-04):
    - First rule: `@layer reset, tokens, base, components, utilities;`
    - `@layer reset` block: minimal reset (box-sizing: border-box on *, margin: 0, padding: 0 on body, img max-width 100%)
    - `@layer base` block: body defaults using tokens (background: var(--bg-primary), color: var(--text-primary), font-family: var(--font-body), line-height: var(--leading-relaxed), font-size: var(--size-base)), link base styles (color: var(--accent), text-decoration-thickness: 1px), selection color (::selection background: var(--accent-15), color: var(--text-primary))
    - `@layer base` block also contains `.grain-overlay` styles: position: fixed, inset: 0, background-image: url('/textures/grain.png'), background-repeat: repeat, background-size: 200px 200px, opacity: 0.04, pointer-events: none, z-index: var(--z-overlay), aria-hidden via HTML attribute (per D-08)
    - `@layer utilities` block: `.sr-only` (screen reader only, standard clip-rect pattern), `.visually-hidden` (alias)
    - `@media (prefers-reduced-motion: reduce)` block at file end: `*, *::before, *::after { animation-duration: 0.01ms !important; animation-iteration-count: 1 !important; transition-duration: 0.01ms !important; scroll-behavior: auto !important; }`

    **src/styles/tokens.css** (per FOUN-02, D-01, D-02, D-03):
    All values inside `@layer tokens { ... }`.

    Light mode `:root` tokens (exact hex values from Visual Direction spec):
    - --bg-primary: #FAF9F6
    - --bg-secondary: #F1F0EE
    - --bg-card: #FFFFFF
    - --text-primary: #1A1A1A
    - --text-secondary: #666666
    - --text-tertiary: #999999
    - --accent: #1B7A3D
    - --accent-hover: #145C2E
    - --accent-5: #F0F7F2
    - --accent-15: #D4EDDB
    - --accent-40: #8CC49E
    - --border-subtle: rgba(0, 0, 0, 0.06)
    - --border-visible: #E5E5E5
    - Spacing scale: --space-1 through --space-9 (4, 8, 16, 24, 32, 48, 64, 96, 128 px)
    - Font tokens: --font-body: var(--font-inter), system-ui, sans-serif; --font-mono: var(--font-jetbrains-mono), 'Courier New', monospace; --font-fallback-sans: system-ui, -apple-system, sans-serif; --font-fallback-mono: 'Courier New', monospace
    - Size scale: --size-xs: 11px, --size-sm: 13px, --size-base: 16px, --size-lg: 18px, --size-xl: 24px, --size-2xl: 32px, --size-3xl: 48px, --size-4xl: 64px
    - Leading: --leading-tight: 1.1, --leading-snug: 1.3, --leading-normal: 1.5, --leading-relaxed: 1.6
    - Tracking: --tracking-tight: -0.02em, --tracking-normal: 0em, --tracking-wide: 0.05em, --tracking-wider: 0.08em
    - Z-index: --z-base: 0, --z-raised: 10, --z-overlay: 100, --z-modal: 200, --z-toast: 300
    - Grid: --grid-cols: 12, --grid-gutter: 24px, --content-max: 1200px, --sidebar-width: 220px

    Dark mode `[data-theme="dark"]` overrides (per D-01 warm charcoal, per D-02 accent unchanged):
    - --bg-primary: #1C1B18
    - --bg-secondary: #252420
    - --bg-card: #2E2D2A
    - --text-primary: #F0EDE6
    - --text-secondary: #A09C95
    - --text-tertiary: #6A6660
    - --border-subtle: rgba(255, 255, 255, 0.06)
    - --border-visible: #3A3935
    - Accent, spacing, typography, z-index, grid tokens are NOT overridden (they stay the same)

    **src/styles/typography.css** (per FOUN-03):
    All values inside `@layer base { ... }`.
    - h1: font-family var(--font-body), font-size var(--size-4xl), font-weight 700, line-height var(--leading-tight), letter-spacing var(--tracking-tight)
    - h2: font-size var(--size-3xl), font-weight 600, line-height var(--leading-tight), letter-spacing var(--tracking-tight)
    - h3: font-size var(--size-2xl), font-weight 600, line-height var(--leading-snug)
    - h4: font-size var(--size-xl), font-weight 600, line-height var(--leading-snug)
    - h5: font-size var(--size-lg), font-weight 600
    - h6: font-size var(--size-base), font-weight 600, text-transform uppercase, letter-spacing var(--tracking-wide), font-family var(--font-mono)
    - p, li: font-size var(--size-base), line-height var(--leading-relaxed)
    - small: font-size var(--size-sm)
    - code, kbd, samp: font-family var(--font-mono), font-size 0.9em
    - A BEM class `.mono-label` for the metadata layer: font-family var(--font-mono), font-size var(--size-xs), text-transform uppercase, letter-spacing var(--tracking-wide), color var(--text-tertiary), font-weight 400

    **src/styles/layout.css** (per FOUN-02):
    All values inside `@layer base { ... }`.
    - `.container` class: max-width var(--content-max), margin-inline auto, padding-inline var(--grid-gutter)
    - `.grid` class: display grid, grid-template-columns repeat(var(--grid-cols), 1fr), gap var(--grid-gutter)
    - Breakpoint custom media comments (no @custom-media support yet, just document the values as CSS comments):
      /* Desktop: min-width 1200px */
      /* Tablet: min-width 768px and max-width 1199px */
      /* Mobile: max-width 767px */
    - Responsive grid: at max-width 767px, .grid has grid-template-columns 1fr

    **src/styles/animations.css** (placeholder for Phase 5):
    `@layer components { /* Animation keyframes added in Phase 5 */ }` with a single comment. Include the prefers-reduced-motion note as a comment.

    **src/layouts/BaseLayout.astro** (per D-03, D-04, D-05, FOUN-05):
    Follow the exact BaseLayout.astro Shell from RESEARCH.md Pattern 4 code example:
    - Import { Font } from 'astro:assets'
    - Import all CSS files in order: global.css first, then tokens.css, typography.css, layout.css, animations.css
    - Props interface: { title: string; description?: string }
    - Default description: 'Portfolio of Adam Palencar, UI/UX Designer'
    - `<html lang="en" data-theme="light">`
    - In `<head>`: meta charset, viewport, title, description, `<Font cssVariable="--font-inter" />`, `<Font cssVariable="--font-jetbrains-mono" />`
    - IMMEDIATELY after Font components: the `is:inline` theme script that reads localStorage and sets data-theme (per D-04). Exact script:
      ```
      <script is:inline>
        (function() {
          const saved = localStorage.getItem('theme');
          const system = window.matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light';
          document.documentElement.dataset.theme = saved ?? system;
        })();
      </script>
      ```
    - In `<body>`: `<div class="grain-overlay" aria-hidden="true"></div>` then `<slot />`
    - After closing `</body>` but before `</html>`: bundled script for astro:after-swap handler (per D-04):
      ```
      <script>
        document.addEventListener('astro:after-swap', () => {
          const saved = localStorage.getItem('theme');
          const system = window.matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light';
          document.documentElement.dataset.theme = saved ?? system;
        });
      </script>
      ```

    **Update src/pages/index.astro** to import and use BaseLayout:
    ```
    ---
    import BaseLayout from '../layouts/BaseLayout.astro';
    ---
    <BaseLayout title="Adam Palencar - UI/UX Designer">
      <main class="container">
        <h1>Portfolio</h1>
        <p>Site under construction.</p>
      </main>
    </BaseLayout>
    ```

    After creating all files, run `npm run build` to verify everything compiles.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `src/styles/global.css` first rule is `@layer reset, tokens, base, components, utilities;`
    - `src/styles/global.css` contains `.grain-overlay`
    - `src/styles/global.css` contains `prefers-reduced-motion: reduce`
    - `src/styles/tokens.css` contains `@layer tokens`
    - `src/styles/tokens.css` contains `--bg-primary: #FAF9F6`
    - `src/styles/tokens.css` contains `--accent: #1B7A3D`
    - `src/styles/tokens.css` contains `[data-theme="dark"]`
    - `src/styles/tokens.css` contains `--bg-primary: #1C1B18` (dark mode)
    - `src/styles/tokens.css` contains `--space-9: 128px`
    - `src/styles/tokens.css` contains `--font-body: var(--font-inter)`
    - `src/styles/typography.css` contains `@layer base`
    - `src/styles/typography.css` contains `.mono-label`
    - `src/styles/layout.css` contains `.container`
    - `src/styles/layout.css` contains `.grid`
    - `src/layouts/BaseLayout.astro` contains `is:inline`
    - `src/layouts/BaseLayout.astro` contains `localStorage.getItem('theme')`
    - `src/layouts/BaseLayout.astro` contains `astro:after-swap`
    - `src/layouts/BaseLayout.astro` contains `Font cssVariable="--font-inter"`
    - `src/layouts/BaseLayout.astro` contains `grain-overlay`
    - `src/pages/index.astro` contains `BaseLayout`
    - `npm run build` exits with code 0
  </acceptance_criteria>
  <done>Complete CSS architecture with @layer declaration, full light+dark token set, typography scale, layout grid, and BaseLayout with Font API integration and two-part dark mode flash prevention. Build passes.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds with zero errors
- `dist/` folder contains generated HTML
- grep confirms `@layer reset, tokens, base, components, utilities` in global.css
- grep confirms `[data-theme="dark"]` in tokens.css
- grep confirms `is:inline` in BaseLayout.astro
- grep confirms `astro:after-swap` in BaseLayout.astro
- No `@import` statements for Fontsource packages anywhere in CSS files
- No `@astrojs/netlify` in package.json
</verification>

<success_criteria>
1. `npm run dev` starts a local Astro server at localhost:4321
2. `npm run build` produces dist/ with zero errors
3. All CSS custom properties defined in tokens.css match the hex values from the design spec
4. Font API configured for both Inter Variable and JetBrains Mono Variable
5. @layer declaration is the first rule in global.css
6. Dark mode token overrides exist under [data-theme="dark"] in tokens.css
7. BaseLayout contains is:inline theme script AND astro:after-swap handler
8. Grain overlay div present in BaseLayout with pointer-events:none in CSS
</success_criteria>

<output>
After completion, create `.planning/phases/01-design-system/01-01-SUMMARY.md`
</output>
