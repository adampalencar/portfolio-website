---
phase: 03-content-infrastructure
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/content/case-studies/orbital-concierge.mdx
  - src/assets/case-studies/orbital-concierge/hero.png
  - src/assets/case-studies/orbital-concierge/gameplay.png
  - src/assets/case-studies/orbital-concierge/spotlight-1-status-manifest.png
  - src/assets/case-studies/orbital-concierge/spotlight-2-elevator-shaft.png
  - src/assets/case-studies/orbital-concierge/spotlight-3-game-board.png
  - src/assets/case-studies/orbital-concierge/design-system-card.png
  - src/assets/case-studies/orbital-concierge/critical-state.png
  - src/assets/case-studies/orbital-concierge/results-screen.png
  - src/assets/case-studies/orbital-concierge/scoring-guide-comparison.png
  - src/assets/case-studies/orbital-concierge/cross-highlighting.png
autonomous: false
requirements: [CONT-04, CONT-05]

must_haves:
  truths:
    - "Orbital Concierge case study is accessible at /work/orbital-concierge"
    - "All 10 screenshots render as optimized images through Astro's pipeline"
    - "Case study follows Minto Pyramid section order: result, problem, key decisions, process, final design, reflection"
    - "Hero image is served as WebP in the production build"
  artifacts:
    - path: "src/content/case-studies/orbital-concierge.mdx"
      provides: "Complete Orbital Concierge case study in MDX format"
      min_lines: 80
      contains: "title: Orbital Concierge"
    - path: "src/assets/case-studies/orbital-concierge/hero.png"
      provides: "Hero screenshot asset"
  key_links:
    - from: "src/content/case-studies/orbital-concierge.mdx"
      to: "src/assets/case-studies/orbital-concierge/"
      via: "import statements for each screenshot"
      pattern: "import.*from.*assets/case-studies/orbital-concierge"
    - from: "src/content/case-studies/orbital-concierge.mdx"
      to: "src/content.config.ts"
      via: "frontmatter validated against Zod schema at build time"
      pattern: "title: Orbital Concierge"
    - from: "src/pages/work/[id].astro"
      to: "src/content/case-studies/orbital-concierge.mdx"
      via: "getCollection('case-studies') discovers the MDX file automatically"
      pattern: "getCollection"
---

<objective>
Author the complete Orbital Concierge case study as MDX with all 10 screenshot assets, proving the content pipeline end-to-end.

Purpose: Validate that the schema, layout, routing, and image optimization all work together on real content. This is the launch case study.
Output: One MDX file with frontmatter, 10 imported screenshots rendered via Astro Image, and a verified production build.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-content-infrastructure/03-RESEARCH.md
@.planning/phases/03-content-infrastructure/03-01-SUMMARY.md

@src/content.config.ts
@src/layouts/CaseStudyLayout.astro
@src/pages/work/[id].astro

<interfaces>
<!-- From Plan 01 outputs -->

From src/content.config.ts (schema fields):
```typescript
// Frontmatter must match:
z.object({
  title: z.string(),
  oneliner: z.string(),
  role: z.string(),
  timeline: z.string(),
  tools: z.array(z.string()),
  category: z.enum(['Personal', 'Professional', 'Academic']),
  tier: z.number().int().min(1).max(3),
  thumbnail: image(),  // relative path to image in src/assets/
  tags: z.array(z.string()),
})
```

From src/layouts/CaseStudyLayout.astro:
- Hero section renders: thumbnail (Image), title (h1), oneliner (p), meta dl (role, timeline, tools)
- Body slot renders MDX content (sections 2-7 of Minto Pyramid)
- CTA section is in layout (not MDX): "Want to work together?" + contact link
- Prev/next nav is in layout, computed from sorted collection in route file
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy assets and author Orbital Concierge MDX</name>
  <files>
    src/assets/case-studies/orbital-concierge/*.png,
    src/content/case-studies/orbital-concierge.mdx
  </files>
  <read_first>
    - C:/Users/mzbir/Desktop/Brain/Personal/02 - Projects/Portfolio Website/Case Studies/Orbital Concierge.md (source content to transcribe)
    - src/content.config.ts (schema fields for frontmatter)
    - src/layouts/CaseStudyLayout.astro (what the layout handles vs what MDX body needs)
  </read_first>
  <action>
1. Copy all 10 PNG screenshots from the vault into the Astro project:
   ```bash
   cp "C:/Users/mzbir/Desktop/Brain/Personal/02 - Projects/Portfolio Website/Case Studies/Assets/Orbital Concierge/final/"*.png src/assets/case-studies/orbital-concierge/
   ```
   Files to copy: hero.png, gameplay.png, spotlight-1-status-manifest.png, spotlight-2-elevator-shaft.png, spotlight-3-game-board.png, design-system-card.png, critical-state.png, results-screen.png, scoring-guide-comparison.png, cross-highlighting.png

2. Create `src/content/case-studies/orbital-concierge.mdx` with this structure:

   **Frontmatter (must match Zod schema exactly):**
   ```yaml
   ---
   title: Orbital Concierge
   oneliner: "I put four competing systems on one screen with no scrolling, no tabs, and no hidden panels."
   role: UI/UX Designer, Developer
   timeline: 2025 to 2026
   tools:
     - Godot 4.5
     - GDScript
     - Figma
   category: Personal
   tier: 1
   thumbnail: ../../assets/case-studies/orbital-concierge/hero.png
   tags:
     - game-ui
     - godot
     - puzzle
   ---
   ```

   **Imports (immediately after frontmatter):**
   ```mdx
   import { Image } from 'astro:assets';
   import heroImg from '../../assets/case-studies/orbital-concierge/hero.png';
   import gameplayImg from '../../assets/case-studies/orbital-concierge/gameplay.png';
   import spotlight1Img from '../../assets/case-studies/orbital-concierge/spotlight-1-status-manifest.png';
   import spotlight2Img from '../../assets/case-studies/orbital-concierge/spotlight-2-elevator-shaft.png';
   import spotlight3Img from '../../assets/case-studies/orbital-concierge/spotlight-3-game-board.png';
   import designSystemImg from '../../assets/case-studies/orbital-concierge/design-system-card.png';
   import criticalStateImg from '../../assets/case-studies/orbital-concierge/critical-state.png';
   import resultsScreenImg from '../../assets/case-studies/orbital-concierge/results-screen.png';
   import scoringGuideImg from '../../assets/case-studies/orbital-concierge/scoring-guide-comparison.png';
   import crossHighlightingImg from '../../assets/case-studies/orbital-concierge/cross-highlighting.png';
   ```

   CRITICAL image path: From `src/content/case-studies/orbital-concierge.mdx`, the relative path to assets is `../../assets/case-studies/orbital-concierge/`. This goes up two directories (content/ then src/) before descending into assets/.

   **MDX body content:** Transcribe the vault case study note (Orbital Concierge.md) into the MDX body. The body contains Minto Pyramid sections 2 through 7 only (hero/meta is handled by CaseStudyLayout, CTA is handled by layout). Use the vault note's exact prose. Replace `{{IMAGE:...}}` placeholders with `<Image>` components:

   Section order in MDX body:
   1. `## Most Playtesters Couldn't Tell Why They Earned Their Score` (The Result)
   2. `## Four Systems, One Screen, No Room to Hide` (The Problem)
   3. `## The Layout Encodes the Decision Sequence` (Key Decision 1: layout)
   4. `## Small Events Get Quiet Feedback, Permanent Ones Get Loud` (Key Decision 2: feedback)
   5. `## Playtesting Rebuilt the Scoring System from Scratch` (Process Evidence)
   6. `## The Shipped Interface` (Final Design)
   7. `## Correct Systems Don't Guarantee a Readable Interface` (Reflection)

   Image placement (matching vault note `{{IMAGE:...}}` markers):
   - After "Most Playtesters..." intro paragraph: NO image (hero is in layout)
   - In "Four Systems..." section: `<Image src={gameplayImg} alt="Level 17 (Cross Traffic), all four passenger types on the board" />`
   - In "The Layout Encodes..." section (three spotlights):
     ```mdx
     <Image src={spotlight1Img} alt="Left panel spotlighted: status manifest showing stops remaining and onboard passengers" />
     <Image src={spotlight2Img} alt="Center shaft spotlighted: elevator position in the building" />
     <Image src={spotlight3Img} alt="Right side spotlighted: waiting passengers at each floor and floor selection buttons" />
     ```
   - In "Small Events..." section:
     ```mdx
     <Image src={criticalStateImg} alt="Cargo at zero tolerance: red border, warning triangle, text color shift" />
     <Image src={resultsScreenImg} alt="Results screen with delivery breakdown, loss penalty, bonuses, 2-star rating" />
     ```
   - In "Playtesting..." section:
     ```mdx
     <Image src={scoringGuideImg} alt="Before and after scoring guide: verbose paragraphs replaced with scannable 3-column table" />
     ```
   - In "The Shipped Interface" section:
     ```mdx
     <Image src={crossHighlightingImg} alt="Hovering Guest row highlights floor 05 on the strip: verify before committing" />
     <Image src={designSystemImg} alt="Typography, color palette, and passenger type system" />
     ```
   - For the missing GIF, add a comment: `{/* TODO: GIF of core gameplay loop, 5-10 seconds looping */}`

   Do NOT include the "Asset Checklist" section from the vault note in the MDX. That's vault metadata, not portfolio content.

   Do NOT include the "Next: another playtest round..." line. Replace the Reflection section's ending with content suitable for a portfolio (constraints, what was learned, what's left to do).

   Do NOT use emdashes or double dashes anywhere in the prose.

3. Run `npm run build` to validate:
   - Zod schema accepts the frontmatter (build fails if any field is wrong type or missing)
   - All 10 image imports resolve (build fails on missing file)
   - Sharp processes images to WebP in dist/_astro/
   - Route generates /work/orbital-concierge page
  </action>
  <verify>
    <automated>cd "C:/Users/mzbir/Desktop/portfolio-website" && npm run build 2>&1 && ls src/assets/case-studies/orbital-concierge/*.png | wc -l && grep -c "import.*from.*assets/case-studies" src/content/case-studies/orbital-concierge.mdx</automated>
  </verify>
  <acceptance_criteria>
    - 10 PNG files exist in src/assets/case-studies/orbital-concierge/
    - src/content/case-studies/orbital-concierge.mdx exists with valid YAML frontmatter matching schema (title, oneliner, role, timeline, tools array, category: Personal, tier: 1, thumbnail path, tags array)
    - MDX file contains 10 import statements from `../../assets/case-studies/orbital-concierge/`
    - MDX file contains `import { Image } from 'astro:assets'`
    - MDX body has 7 h2 headings in Minto Pyramid order
    - MDX contains `{/* TODO: GIF` comment for missing gameplay GIF
    - MDX does NOT contain "Asset Checklist" section or emdashes
    - `npm run build` exits 0 with zero errors
    - dist/ output contains .webp files in _astro/ directory (Sharp optimization working)
  </acceptance_criteria>
  <done>Orbital Concierge case study authored as MDX with all 10 screenshots imported via Astro Image. Build succeeds, images optimized to WebP, page generated at /work/orbital-concierge.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of case study at all breakpoints</name>
  <files>none</files>
  <read_first>
    - src/content/case-studies/orbital-concierge.mdx
    - src/layouts/CaseStudyLayout.astro
  </read_first>
  <action>
    Start the dev server with `npm run dev`. Present the user with verification steps for http://localhost:4321/work/orbital-concierge at desktop (1440px+), tablet (768px), and mobile (375px) breakpoints. Include dark mode toggle check and sidebar navigation check.
  </action>
  <what-built>
    Complete Orbital Concierge case study page with:
    - Hero image, title, one-liner, and role/timeline/tools metadata
    - Full case study body with 7 Minto Pyramid sections
    - 10 optimized screenshot images inline
    - CTA section ("Want to work together?")
    - Prev/next navigation (absent when only one case study exists)
    - Responsive layout at desktop, tablet, and mobile
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and open http://localhost:4321/work/orbital-concierge
    2. Desktop (1440px+):
       - Hero image loads full-width, sharp, no layout shift
       - Title "Orbital Concierge" and one-liner below hero
       - Role/Timeline/Tools in monospace metadata row
       - 7 sections with descriptive headings, prose reads naturally
       - All 10 screenshots display inline, full-width, with rounded corners
       - CTA section at bottom: "Want to work together?" with contact link
       - No prev/next nav (only one case study exists)
    3. Tablet (768px):
       - Content fills viewport width appropriately
       - Images scale down, no horizontal overflow
       - Metadata row wraps cleanly
    4. Mobile (375px):
       - Title scales to smaller size
       - Images full-width
       - Comfortable reading line length
       - No horizontal scroll
    5. Navigate to /work, confirm Orbital Concierge link still works from sidebar
    6. Toggle dark mode: verify case study text, backgrounds, and borders adapt
  </how-to-verify>
  <verify>
    <automated>cd "C:/Users/mzbir/Desktop/portfolio-website" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - User confirms visual quality at all three breakpoints (desktop 1440px+, tablet 768px, mobile 375px)
    - User confirms dark mode renders correctly on case study page
    - User confirms sidebar navigation to /work/orbital-concierge works
    - User confirms all 10 screenshots display inline
  </acceptance_criteria>
  <done>Visual verification approved by user at desktop, tablet, and mobile breakpoints with dark mode confirmed.</done>
  <resume-signal>Type "approved" or describe any visual issues to fix</resume-signal>
</task>

</tasks>

<verification>
After both tasks:
1. `npm run build` succeeds with zero errors
2. `/work/orbital-concierge` is accessible in the browser
3. All 10 screenshots render as optimized WebP images
4. Case study follows Minto Pyramid section order
5. Hero, metadata, CTA, and prev/next nav all render correctly from CaseStudyLayout
6. Dark mode applies correctly to all case study elements
7. Responsive layout works at desktop, tablet, and mobile breakpoints
</verification>

<success_criteria>
- Orbital Concierge is a complete, readable case study at /work/orbital-concierge
- All images pass through Astro's Sharp pipeline (WebP in dist/_astro/)
- The content pipeline is proven: schema validates, MDX compiles, images optimize, route generates
- Visual verification approved at all three breakpoints
</success_criteria>

<output>
After completion, create `.planning/phases/03-content-infrastructure/03-02-SUMMARY.md`
</output>
