---
phase: 03-content-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - astro.config.mjs
  - src/content.config.ts
  - src/layouts/CaseStudyLayout.astro
  - src/pages/work/[id].astro
  - src/pages/work/orbital-concierge.astro
  - src/styles/case-study.css
autonomous: true
requirements: [CONT-01, CONT-02, CONT-03, CONT-05]

must_haves:
  truths:
    - "A content.config.ts Zod schema validates case study frontmatter at build time"
    - "CaseStudyLayout renders hero, metadata, MDX body slot, and prev/next navigation"
    - "Dynamic route [id].astro generates pages from getCollection() with getStaticPaths()"
    - "Prev/next navigation computes adjacent projects sorted by tier then id"
  artifacts:
    - path: "src/content.config.ts"
      provides: "Case study collection schema with Zod validation and image() helper"
      contains: "defineCollection"
      exports: ["collections"]
    - path: "src/layouts/CaseStudyLayout.astro"
      provides: "Minto Pyramid case study template wrapping BaseLayout"
      min_lines: 60
    - path: "src/pages/work/[id].astro"
      provides: "Dynamic route generating case study pages from content collection"
      contains: "getStaticPaths"
    - path: "src/styles/case-study.css"
      provides: "BEM styles for case study layout"
      contains: "case-study__hero"
  key_links:
    - from: "src/pages/work/[id].astro"
      to: "src/layouts/CaseStudyLayout.astro"
      via: "import and render with entry, prev, next props"
      pattern: "CaseStudyLayout"
    - from: "src/pages/work/[id].astro"
      to: "src/content.config.ts"
      via: "getCollection('case-studies')"
      pattern: "getCollection"
    - from: "src/content.config.ts"
      to: "astro.config.mjs"
      via: "mdx() integration enables .mdx file processing"
      pattern: "mdx"
---

<objective>
Wire the complete case study content infrastructure: MDX integration, Zod schema, CaseStudyLayout template, and dynamic routing.

Purpose: Establish the content pipeline so that dropping an MDX file into src/content/case-studies/ automatically generates a styled, routed case study page with prev/next navigation.
Output: Four new files (content.config.ts, CaseStudyLayout.astro, [id].astro, case-study.css) plus updated astro.config.mjs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-content-infrastructure/03-RESEARCH.md

@src/layouts/BaseLayout.astro
@src/styles/tokens.css
@astro.config.mjs

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/layouts/BaseLayout.astro:
```typescript
interface Props {
  title: string;
  description?: string;
}
```
BaseLayout wraps all pages with ClientRouter, theme script, sidebar, topbar, mobile drawer, footer. The main content goes into `<slot />` inside `.site-layout__main`.

From src/styles/tokens.css:
```css
/* Key tokens for case study styling */
--bg-primary: #FAF9F6;        --text-primary: #1A1A1A;
--text-secondary: #666666;     --accent: #1B7A3D;
--space-3: 16px; --space-4: 24px; --space-5: 32px; --space-6: 48px; --space-7: 64px;
--font-body: var(--font-inter), system-ui, sans-serif;
--font-mono: var(--font-jetbrains-mono), 'Courier New', monospace;
--size-sm: 13px; --size-base: 16px; --size-lg: 18px; --size-xl: 24px; --size-2xl: 32px; --size-3xl: 48px;
--content-max: 1200px;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install MDX integration and create content schema</name>
  <files>astro.config.mjs, src/content.config.ts, package.json</files>
  <read_first>
    - astro.config.mjs (current integrations array)
    - package.json (current dependencies)
    - src/styles/tokens.css (token reference)
  </read_first>
  <action>
1. Install the MDX integration:
   ```bash
   npx astro add mdx --yes
   ```
   This auto-adds `@astrojs/mdx` to package.json and adds `mdx()` to the integrations array in astro.config.mjs. Verify the import `import mdx from '@astrojs/mdx';` appears at the top and `mdx()` is in the integrations array alongside `icon()`.

2. Create `src/content.config.ts` with this exact content:
   ```typescript
   import { defineCollection } from 'astro:content';
   import { glob } from 'astro/loaders';
   import { z } from 'astro/zod';

   const caseStudies = defineCollection({
     loader: glob({ pattern: '**/*.mdx', base: './src/content/case-studies' }),
     schema: ({ image }) => z.object({
       title: z.string(),
       oneliner: z.string(),
       role: z.string(),
       timeline: z.string(),
       tools: z.array(z.string()),
       category: z.enum(['Personal', 'Professional', 'Academic']),
       tier: z.number().int().min(1).max(3),
       thumbnail: image(),
       tags: z.array(z.string()),
     }),
   });

   export const collections = { 'case-studies': caseStudies };
   ```

   CRITICAL: The `image` function is destructured from the schema callback argument `({ image })`, NOT imported from `astro/zod`. This is the Astro 6 pattern for frontmatter image path resolution.

3. Create the content directory: `src/content/case-studies/` (empty for now, Plan 02 adds the MDX file).

4. Create the assets directory: `src/assets/case-studies/orbital-concierge/` (empty for now, Plan 02 copies screenshots).
  </action>
  <verify>
    <automated>cd "C:/Users/mzbir/Desktop/portfolio-website" && node -e "const pkg = require('./package.json'); console.log('@astrojs/mdx:', pkg.dependencies['@astrojs/mdx'] || pkg.devDependencies['@astrojs/mdx'] || 'NOT FOUND')" && grep -c "mdx()" astro.config.mjs && test -f src/content.config.ts && echo "content.config.ts exists" && grep -c "defineCollection" src/content.config.ts && test -d src/content/case-studies && echo "content dir exists" && test -d src/assets/case-studies/orbital-concierge && echo "assets dir exists"</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains `@astrojs/mdx` in dependencies or devDependencies
    - astro.config.mjs contains `import mdx from '@astrojs/mdx'` and `mdx()` in integrations array
    - src/content.config.ts exists and contains `defineCollection`, `glob`, `schema: ({ image })`, `export const collections`
    - src/content/case-studies/ directory exists
    - src/assets/case-studies/orbital-concierge/ directory exists
  </acceptance_criteria>
  <done>MDX integration installed, content schema defined with all 10 frontmatter fields including image() helper for thumbnail, directories created for content and assets</done>
</task>

<task type="auto">
  <name>Task 2: Create CaseStudyLayout and case study CSS</name>
  <files>src/layouts/CaseStudyLayout.astro, src/styles/case-study.css</files>
  <read_first>
    - src/layouts/BaseLayout.astro (wrapper pattern, Props interface)
    - src/styles/tokens.css (all design tokens)
    - src/styles/typography.css (existing type classes)
    - src/styles/layout.css (existing layout patterns)
  </read_first>
  <action>
1. Create `src/styles/case-study.css` with BEM styles wrapped in `@layer components`. Use existing design tokens exclusively. Key classes:

   ```css
   @layer components {
     .case-study {
       max-width: var(--content-max);
       margin: 0 auto;
       padding: var(--space-6) var(--space-4);
     }

     .case-study__hero {
       margin-bottom: var(--space-7);
     }

     .case-study__hero-image {
       width: 100%;
       height: auto;
       border-radius: 4px;
       margin-bottom: var(--space-5);
     }

     .case-study__title {
       font-family: var(--font-body);
       font-size: var(--size-3xl);
       font-weight: 700;
       line-height: var(--leading-tight);
       letter-spacing: var(--tracking-tight);
       color: var(--text-primary);
       margin-bottom: var(--space-3);
     }

     .case-study__oneliner {
       font-family: var(--font-body);
       font-size: var(--size-lg);
       line-height: var(--leading-relaxed);
       color: var(--text-secondary);
       margin-bottom: var(--space-5);
       max-width: 720px;
     }

     .case-study__meta {
       display: flex;
       flex-wrap: wrap;
       gap: var(--space-4);
       font-family: var(--font-mono);
       font-size: var(--size-sm);
       color: var(--text-secondary);
       letter-spacing: var(--tracking-wide);
       text-transform: uppercase;
       margin: 0;
       padding: 0;
     }

     .case-study__meta dt {
       font-weight: 600;
       color: var(--text-tertiary);
       margin-bottom: var(--space-1);
     }

     .case-study__meta dd {
       margin: 0;
     }

     .case-study__meta-item {
       display: flex;
       flex-direction: column;
     }

     /* MDX body content */
     .case-study__body {
       max-width: 720px;
     }

     .case-study__body h2 {
       font-family: var(--font-body);
       font-size: var(--size-2xl);
       font-weight: 600;
       line-height: var(--leading-snug);
       color: var(--text-primary);
       margin-top: var(--space-7);
       margin-bottom: var(--space-4);
     }

     .case-study__body h3 {
       font-family: var(--font-body);
       font-size: var(--size-xl);
       font-weight: 600;
       line-height: var(--leading-snug);
       color: var(--text-primary);
       margin-top: var(--space-6);
       margin-bottom: var(--space-3);
     }

     .case-study__body p {
       font-family: var(--font-body);
       font-size: var(--size-base);
       line-height: var(--leading-relaxed);
       color: var(--text-primary);
       margin-bottom: var(--space-4);
     }

     .case-study__body ul,
     .case-study__body ol {
       padding-left: var(--space-4);
       margin-bottom: var(--space-4);
     }

     .case-study__body li {
       font-size: var(--size-base);
       line-height: var(--leading-relaxed);
       margin-bottom: var(--space-2);
     }

     .case-study__body strong {
       font-weight: 600;
       color: var(--text-primary);
     }

     /* Images inside MDX body rendered by <Image> */
     .case-study__body :global(img) {
       width: 100%;
       height: auto;
       border-radius: 4px;
       margin: var(--space-5) 0;
     }

     .case-study__body hr {
       border: none;
       border-top: 1px solid var(--border-subtle);
       margin: var(--space-7) 0;
     }

     /* Prev/Next navigation */
     .case-study__nav {
       display: flex;
       justify-content: space-between;
       align-items: center;
       padding-top: var(--space-6);
       margin-top: var(--space-7);
       border-top: 1px solid var(--border-visible);
     }

     .case-study__nav a {
       font-family: var(--font-mono);
       font-size: var(--size-sm);
       color: var(--accent);
       text-decoration: none;
       letter-spacing: var(--tracking-wide);
       text-transform: uppercase;
       transition: color 0.15s ease;
     }

     .case-study__nav a:hover {
       color: var(--accent-hover);
     }

     .case-study__nav-prev::before {
       content: '\2190\00a0';
     }

     .case-study__nav-next {
       margin-left: auto;
     }

     .case-study__nav-next::after {
       content: '\00a0\2192';
     }

     /* CTA section after MDX body */
     .case-study__cta {
       text-align: center;
       padding: var(--space-7) 0;
       margin-top: var(--space-7);
       border-top: 1px solid var(--border-subtle);
     }

     .case-study__cta-heading {
       font-family: var(--font-body);
       font-size: var(--size-xl);
       font-weight: 600;
       color: var(--text-primary);
       margin-bottom: var(--space-3);
     }

     .case-study__cta-text {
       font-family: var(--font-body);
       font-size: var(--size-base);
       color: var(--text-secondary);
       margin-bottom: var(--space-4);
     }

     .case-study__cta-link {
       font-family: var(--font-mono);
       font-size: var(--size-sm);
       color: var(--accent);
       text-decoration: none;
       letter-spacing: var(--tracking-wide);
       text-transform: uppercase;
     }

     .case-study__cta-link:hover {
       color: var(--accent-hover);
     }

     /* Responsive */
     @media (max-width: 768px) {
       .case-study {
         padding: var(--space-5) var(--space-3);
       }

       .case-study__title {
         font-size: var(--size-2xl);
       }

       .case-study__meta {
         gap: var(--space-3);
       }

       .case-study__body h2 {
         font-size: var(--size-xl);
       }

       .case-study__nav {
         flex-direction: column;
         gap: var(--space-3);
         align-items: flex-start;
       }

       .case-study__nav-next {
         margin-left: 0;
       }
     }
   }
   ```

2. Create `src/layouts/CaseStudyLayout.astro`:

   ```astro
   ---
   import { Image } from 'astro:assets';
   import type { CollectionEntry } from 'astro:content';
   import BaseLayout from './BaseLayout.astro';
   import '../styles/case-study.css';

   interface Props {
     entry: CollectionEntry<'case-studies'>;
     prev: CollectionEntry<'case-studies'> | null;
     next: CollectionEntry<'case-studies'> | null;
   }

   const { entry, prev, next } = Astro.props;
   const { title, role, timeline, tools, thumbnail, oneliner } = entry.data;
   ---
   <BaseLayout title={`${title} - Adam Palencar`} description={oneliner}>
     <article class="case-study">
       <header class="case-study__hero">
         <Image
           src={thumbnail}
           alt={`${title} hero image`}
           class="case-study__hero-image"
           widths={[600, 900, 1200]}
           sizes="(max-width: 768px) 100vw, (max-width: 1199px) 100vw, 1200px"
           loading="eager"
         />
         <h1 class="case-study__title">{title}</h1>
         <p class="case-study__oneliner">{oneliner}</p>
         <dl class="case-study__meta">
           <div class="case-study__meta-item">
             <dt>Role</dt>
             <dd>{role}</dd>
           </div>
           <div class="case-study__meta-item">
             <dt>Timeline</dt>
             <dd>{timeline}</dd>
           </div>
           <div class="case-study__meta-item">
             <dt>Tools</dt>
             <dd>{tools.join(', ')}</dd>
           </div>
         </dl>
       </header>
       <div class="case-study__body">
         <slot />
       </div>
       <section class="case-study__cta">
         <h2 class="case-study__cta-heading">Want to work together?</h2>
         <p class="case-study__cta-text">I design interfaces that communicate. Let's talk.</p>
         <a href="/#contact" class="case-study__cta-link">Get in touch</a>
       </section>
       {(prev || next) && (
         <nav class="case-study__nav" aria-label="Case study navigation">
           {prev && <a href={`/work/${prev.id}`} class="case-study__nav-prev">{prev.data.title}</a>}
           {next && <a href={`/work/${next.id}`} class="case-study__nav-next">{next.data.title}</a>}
         </nav>
       )}
     </article>
   </BaseLayout>
   ```

   Key design decisions:
   - CTA is in the layout (structural, same on every case study), NOT in the MDX body. MDX body ends at Reflection.
   - Prev/next nav receives sorted entries as props from the route file. No getCollection() call in the layout.
   - Hero image uses `widths={[600, 900, 1200]}` and responsive `sizes` for optimal loading.
   - `loading="eager"` on hero image since it's above the fold (LCP candidate).
   - Meta items wrapped in `<div class="case-study__meta-item">` for flex layout grouping.
  </action>
  <verify>
    <automated>cd "C:/Users/mzbir/Desktop/portfolio-website" && test -f src/layouts/CaseStudyLayout.astro && echo "layout exists" && grep -c "CollectionEntry" src/layouts/CaseStudyLayout.astro && grep -c "case-study__hero" src/layouts/CaseStudyLayout.astro && test -f src/styles/case-study.css && echo "css exists" && grep -c "case-study__nav" src/styles/case-study.css && grep -c "@layer components" src/styles/case-study.css</automated>
  </verify>
  <acceptance_criteria>
    - src/layouts/CaseStudyLayout.astro exists and contains `CollectionEntry<'case-studies'>` in Props interface
    - CaseStudyLayout imports BaseLayout and wraps content in it
    - CaseStudyLayout contains `case-study__hero`, `case-study__body` with `<slot />`, `case-study__cta`, and `case-study__nav`
    - CaseStudyLayout receives `entry`, `prev`, `next` as Props (NOT calling getCollection internally)
    - src/styles/case-study.css exists, wrapped in `@layer components`, contains BEM classes: `case-study__hero`, `case-study__title`, `case-study__oneliner`, `case-study__meta`, `case-study__body`, `case-study__nav`, `case-study__cta`
    - CSS uses only existing design tokens from tokens.css (no hardcoded colors or spacing values)
    - Responsive breakpoint at 768px adjusts title size, padding, and nav layout
  </acceptance_criteria>
  <done>CaseStudyLayout renders hero image, title, one-liner, role/timeline/tools metadata, MDX body via slot, CTA section, and prev/next navigation. All styled with BEM classes using design tokens.</done>
</task>

<task type="auto">
  <name>Task 3: Create dynamic route and delete static placeholder</name>
  <files>src/pages/work/[id].astro, src/pages/work/orbital-concierge.astro</files>
  <read_first>
    - src/pages/work/orbital-concierge.astro (static placeholder to delete)
    - src/content.config.ts (schema for getCollection type inference)
    - src/layouts/CaseStudyLayout.astro (Props interface: entry, prev, next)
  </read_first>
  <action>
1. Delete the static placeholder file `src/pages/work/orbital-concierge.astro`. It conflicts with the dynamic route (static routes take priority over dynamic in Astro, causing the placeholder to silently win).

2. Create `src/pages/work/[id].astro` with this exact content:

   ```astro
   ---
   import { getCollection, render } from 'astro:content';
   import CaseStudyLayout from '../../layouts/CaseStudyLayout.astro';

   export async function getStaticPaths() {
     const studies = await getCollection('case-studies');
     return studies.map(entry => ({
       params: { id: entry.id },
       props: { entry },
     }));
   }

   const { entry } = Astro.props;
   const { Content } = await render(entry);

   const allStudies = await getCollection('case-studies');
   const sorted = allStudies.sort((a, b) =>
     a.data.tier !== b.data.tier
       ? a.data.tier - b.data.tier
       : a.id.localeCompare(b.id)
   );
   const idx = sorted.findIndex(s => s.id === entry.id);
   const prev = idx > 0 ? sorted[idx - 1] : null;
   const next = idx < sorted.length - 1 ? sorted[idx + 1] : null;
   ---
   <CaseStudyLayout entry={entry} prev={prev} next={next}>
     <Content />
   </CaseStudyLayout>
   ```

   Key details:
   - Uses `[id].astro` (NOT rest spread `[...id].astro`) since case study slugs are flat, no nesting.
   - `getStaticPaths()` generates one page per MDX file in the collection.
   - Prev/next sorting: by tier (ascending) then by id (alphabetical) for stable order.
   - `render(entry)` compiles the MDX content to a `Content` component.
   - The `Content` component is passed into CaseStudyLayout's `<slot />`.

   NOTE: This route will not produce any pages until Plan 02 adds the first MDX file. The build will succeed with zero case study pages generated (empty collection is valid).
  </action>
  <verify>
    <automated>cd "C:/Users/mzbir/Desktop/portfolio-website" && test -f src/pages/work/[id].astro && echo "[id].astro exists" && test ! -f src/pages/work/orbital-concierge.astro && echo "placeholder deleted" && grep -c "getStaticPaths" "src/pages/work/[id].astro" && grep -c "getCollection" "src/pages/work/[id].astro" && grep -c "CaseStudyLayout" "src/pages/work/[id].astro"</automated>
  </verify>
  <acceptance_criteria>
    - src/pages/work/[id].astro exists and contains `getStaticPaths`, `getCollection('case-studies')`, `render(entry)`, `CaseStudyLayout`
    - src/pages/work/orbital-concierge.astro does NOT exist (deleted)
    - Route file sorts studies by `tier` then `id` for prev/next computation
    - Route file passes `entry`, `prev`, `next` as props to CaseStudyLayout
    - `npm run build` succeeds with zero errors (empty collection is valid)
  </acceptance_criteria>
  <done>Dynamic route generates case study pages from content collection. Static placeholder deleted. Prev/next navigation computed from sorted collection and passed to layout as props.</done>
</task>

</tasks>

<verification>
After all three tasks:
1. `npm run build` succeeds with zero errors (empty collection produces no case study pages, which is valid)
2. `src/content.config.ts` defines a `case-studies` collection with Zod schema including `image()` helper
3. `src/layouts/CaseStudyLayout.astro` wraps BaseLayout with hero, body slot, CTA, and prev/next nav
4. `src/pages/work/[id].astro` uses getStaticPaths + getCollection for dynamic routing
5. No static placeholder conflicts remain
</verification>

<success_criteria>
- `npm run build` exits 0
- Content schema, layout, dynamic route, and CSS all exist at their specified paths
- CaseStudyLayout follows Minto Pyramid structure: hero frame, body slot (for MDX sections 2-7), CTA, prev/next nav
- All CSS uses existing design tokens with BEM naming in @layer components
</success_criteria>

<output>
After completion, create `.planning/phases/03-content-infrastructure/03-01-SUMMARY.md`
</output>
