---
phase: 04-pages
plan: 04
type: execute
wave: 3
depends_on:
  - 04-02
  - 04-03
files_modified:
  - src/pages/404.astro
  - src/pages/accessibility.astro
  - netlify.toml
  - public/_redirects
autonomous: false
requirements:
  - PAGE-05
  - PAGE-06
must_haves:
  truths:
    - "Visiting a non-existent route shows the custom 404 page with dry/witty copy"
    - "404 page has navigation links back to Home and Work"
    - "Accessibility statement page exists at /accessibility with WCAG 2.2 AA conformance details"
    - "netlify.toml configures build command, publish directory, and Node 22"
    - "public/_redirects handles 404 catch-all for Netlify"
  artifacts:
    - path: "src/pages/404.astro"
      provides: "Custom 404 page with personality"
      contains: "404"
    - path: "src/pages/accessibility.astro"
      provides: "WCAG 2.2 AA accessibility statement"
      contains: "WCAG"
    - path: "netlify.toml"
      provides: "Netlify build configuration"
      contains: "npm run build"
    - path: "public/_redirects"
      provides: "Netlify 404 catch-all redirect"
      contains: "/* /404 404"
  key_links:
    - from: "src/pages/404.astro"
      to: "src/layouts/BaseLayout.astro"
      via: "layout wrapper"
      pattern: "import BaseLayout"
    - from: "netlify.toml"
      to: "dist"
      via: "publish directory setting"
      pattern: "publish"
---

<objective>
Build the 404 page, accessibility statement, and Netlify deployment configuration. These are the final pieces needed before the site can go live.

Purpose: The 404 page adds personality to dead ends. The accessibility statement signals professional awareness of WCAG standards. The Netlify config enables the first live deploy, completing Phase 4's goal of "site is live on Netlify for the first time."

Output: 404.astro, accessibility.astro, netlify.toml, public/_redirects.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
From src/layouts/BaseLayout.astro:
```typescript
interface Props {
  title: string;
  description?: string;
}
```

From src/styles/tokens.css (relevant tokens):
```css
--bg-primary: #FAF9F6; --bg-secondary: #F1F0EE;
--text-primary: #1A1A1A; --text-secondary: #666666; --text-tertiary: #999999;
--accent: #1B7A3D; --accent-hover: #145C2E;
--border-visible: #E5E5E5;
--space-2: 8px; --space-3: 16px; --space-4: 24px;
--space-5: 32px; --space-7: 64px; --space-8: 96px;
--size-xs: 11px; --size-sm: 13px; --size-base: 16px;
--size-lg: 18px; --size-xl: 24px;
--font-mono: var(--font-jetbrains-mono), 'Courier New', monospace;
--content-max: 1200px; --grid-gutter: 24px;
--tracking-wide: 0.05em;
```

From astro.config.mjs:
```javascript
site: 'https://adampalencar.netlify.app'
output: 'static'
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 404 page, accessibility statement, and Netlify config</name>
  <files>
    src/pages/404.astro,
    src/pages/accessibility.astro,
    netlify.toml,
    public/_redirects
  </files>
  <read_first>
    - src/layouts/BaseLayout.astro (layout wrapper for new pages)
    - src/styles/tokens.css (design tokens)
    - astro.config.mjs (site URL, output mode)
  </read_first>
  <action>
**File 1: `src/pages/404.astro`** (per D-14: dry/witty tone, elevator reference)

Wrap in BaseLayout with title "404 - Page Not Found". Structure:
- `section.error-page` containing:
  - `span.error-page__code.mono-label` with text "ERROR 404"
  - `h1.error-page__heading` with text "The elevator doesn't stop at this floor."
  - `p.error-page__body` with text "Whatever you were looking for, it's not between these walls. Try the lobby or check the directory."
  - `nav.error-page__nav` with two links:
    - `a.error-page__link` href="/" text "Return to Lobby" (filled green button)
    - `a.error-page__link.error-page__link--outline` href="/work" text "View the Work" (outline button)

Scoped CSS (@layer components):
- `.error-page`: `max-width: 640px`, `margin: var(--space-8) auto`, `padding: 0 var(--grid-gutter)`, `text-align: center`
- `.error-page__code`: `font-family: var(--font-mono)`, `font-size: var(--size-xs)`, `text-transform: uppercase`, `letter-spacing: var(--tracking-wide)`, `color: var(--text-tertiary)`, `display: block`, `margin-bottom: var(--space-3)`
- `.error-page__heading`: `margin-bottom: var(--space-3)`
- `.error-page__body`: `color: var(--text-secondary)`, `font-size: var(--size-lg)`, `line-height: 1.6`, `margin-bottom: var(--space-5)`
- `.error-page__nav`: `display: flex`, `gap: var(--space-3)`, `justify-content: center`
- `.error-page__link`: `display: inline-block`, `padding: var(--space-2) var(--space-4)`, `background: var(--accent)`, `color: #fff`, `text-decoration: none`, `border-radius: 4px`, `font-family: var(--font-mono)`, `font-size: var(--size-sm)`, `letter-spacing: var(--tracking-wide)`, `text-transform: uppercase`, `transition: background 0.2s ease`
- `.error-page__link:hover`: `background: var(--accent-hover)`
- `.error-page__link--outline`: `background: transparent`, `color: var(--accent)`, `border: 1px solid var(--accent)`
- `.error-page__link--outline:hover`: `background: var(--accent-5)`

**File 2: `src/pages/accessibility.astro`** (per D-15: standard WCAG 2.2 AA template)

Wrap in BaseLayout with title "Accessibility Statement - Adam Palencar" and description "Accessibility commitment and WCAG 2.2 AA conformance details."

Structure inside `article.a11y-statement`:
- h1: "Accessibility Statement"
- h2: "Commitment" + paragraph: "I am committed to making this website accessible to everyone, including people with disabilities. I aim to conform to the Web Content Accessibility Guidelines (WCAG) 2.2 at Level AA."
- h2: "Conformance Status" + paragraph: "This website is partially conformant with WCAG 2.2 Level AA. 'Partially conformant' means that some parts of the content may not fully conform to the standard."
- h2: "Measures Taken" + unordered list with items: "Semantic HTML landmarks throughout (header, nav, main, footer)", "Logical heading hierarchy with no skipped levels", "Keyboard-navigable interface with visible focus states", "Sufficient color contrast ratios (4.5:1 body text, 3:1 large text)", "Skip-to-content link for keyboard users", "Responsive design tested at multiple breakpoints", "Reduced motion support via prefers-reduced-motion media query", "Alt text provided for all meaningful images"
- h2: "Known Limitations" + unordered list: "Some third-party tool icons in the About page tools section may lack detailed alt text descriptions", "PDF resume may not be fully screen-reader accessible"
- h2: "Feedback" + paragraph: "If you encounter any accessibility barriers on this site, please contact me. I take accessibility feedback seriously and will do my best to address issues promptly." + email link to adampalencar25@gmail.com
- h2: "Assessment" + paragraph: "This statement was last updated on April 2026. Self-assessment was performed against WCAG 2.2 Level AA criteria."

Scoped CSS (@layer components):
- `.a11y-statement`: `max-width: 720px`, `margin-inline: auto`, `padding: var(--space-7) var(--grid-gutter) var(--space-8)`
- `.a11y-statement h2`: `margin-top: var(--space-6)`, `margin-bottom: var(--space-3)`
- `.a11y-statement p`: `color: var(--text-secondary)`, `margin-bottom: var(--space-3)`
- `.a11y-statement ul`: `color: var(--text-secondary)`, `padding-left: var(--space-4)`, `margin-bottom: var(--space-4)`
- `.a11y-statement li`: `margin-bottom: var(--space-2)`
- `.a11y-statement a`: `color: var(--accent)`, `text-decoration: underline`

**File 3: `netlify.toml`** (project root)

```toml
[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
```

**File 4: `public/_redirects`**

Single line content: `/* /404 404`

This tells Netlify to serve the built 404.html page for any unmatched route.
  </action>
  <verify>
    <automated>cd C:/Users/mzbir/Desktop/portfolio-website && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/pages/404.astro exists and contains "The elevator doesn't stop at this floor"
    - src/pages/404.astro contains href="/" and href="/work" for navigation links
    - src/pages/accessibility.astro exists and contains "WCAG 2.2"
    - src/pages/accessibility.astro contains "adampalencar25@gmail.com"
    - netlify.toml exists at project root and contains command = "npm run build"
    - netlify.toml contains publish = "dist"
    - netlify.toml contains NODE_VERSION = "22"
    - public/_redirects exists and contains /* /404 404
    - npm run build completes with zero errors
  </acceptance_criteria>
  <done>404 page renders with dry/witty elevator-themed copy and nav links. Accessibility statement exists at /accessibility with WCAG 2.2 AA conformance details. Netlify config is ready for first deploy.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify all pages and first Netlify deploy</name>
  <files>src/pages/404.astro, src/pages/accessibility.astro</files>
  <action>User visually verifies all Phase 4 pages across breakpoints and approves the first Netlify deploy.</action>
  <what-built>All Phase 4 pages: homepage (hero, featured cards, intro, contact strip), work page (tiered grid), about page (bio, timeline, tools grid, personality), 404 page, accessibility statement. Netlify configuration for first deploy.</what-built>
  <how-to-verify>
    1. Run `npm run dev` and open http://localhost:4321
    2. Homepage: verify hero section (name, role, statement, CTA), 3 featured cards (Krom, Pipeline, OC), intro section, contact strip at bottom
    3. Click "Contact" in sidebar nav from any page: should scroll to contact strip on homepage
    4. Work page: verify cards flow in a continuous grid (T1 full-width, T2 two-up, T3 three-up when more tiers exist). No tier labels.
    5. About page: verify photo placeholder (AP initials), bio text, experience timeline with dots/line, education, tools icon grid (17 icons), personality section
    6. Visit /nonexistent-path to see 404 page with elevator copy and nav links
    7. Visit /accessibility to see WCAG 2.2 AA statement
    8. Check all pages at 3 breakpoints: desktop (1440px), tablet (768px), mobile (375px)
    9. Toggle dark mode on each page: verify tokens swap correctly
    10. Deploy to Netlify: connect repo, push, verify live URL loads
  </how-to-verify>
  <verify>User confirms all pages render correctly across breakpoints and dark mode</verify>
  <done>User approves all pages visually and first Netlify deploy succeeds</done>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` succeeds with zero errors
- All 6 page routes exist: /, /work, /about, /accessibility, /404, and case study routes
- netlify.toml has correct build config for first deploy
- 404 catch-all redirect is in place
</verification>

<success_criteria>
- 404 page displays with dry/witty copy and navigation to Home and Work
- Accessibility statement exists with WCAG 2.2 AA conformance details
- Netlify config is ready for deployment with Node 22, correct build command and publish dir
- User approves all pages visually across breakpoints and deploys to Netlify
</success_criteria>

<output>
After completion, create `.planning/phases/04-pages/04-04-SUMMARY.md`
</output>
