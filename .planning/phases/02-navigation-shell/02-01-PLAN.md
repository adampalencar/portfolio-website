---
phase: 02-navigation-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - astro.config.mjs
  - src/icons/home.svg
  - src/icons/briefcase.svg
  - src/icons/user.svg
  - src/icons/mail.svg
  - src/icons/moon.svg
  - src/icons/sun.svg
  - src/icons/cloud-sun.svg
  - src/icons/chevron-down.svg
  - src/icons/menu.svg
  - src/icons/close.svg
  - src/icons/download.svg
  - src/icons/link.svg
  - src/icons/map-pin.svg
  - src/layouts/BaseLayout.astro
  - src/components/layout/SkipLink.astro
  - src/components/layout/Sidebar.astro
  - src/components/ui/ThemeToggle.astro
  - src/pages/work/index.astro
  - src/pages/work/orbital-concierge.astro
  - src/pages/about.astro
autonomous: true
requirements:
  - NAV-01
  - NAV-02
  - NAV-03
  - NAV-04
  - NAV-05
  - NAV-06
  - LAY-01

must_haves:
  truths:
    - "astro-icon <Icon> component renders SVGs from src/icons/ at build time"
    - "BaseLayout includes ClientRouter for View Transitions"
    - "Skip-to-content link targets #main-content and is visible on focus"
    - "Desktop sidebar (1200px+) is 220px wide, sticky, with 4 nav items and pixelarticons"
    - "Work folder auto-expands on case study pages with CSS grid-template-rows accordion"
    - "Dark mode toggle swaps sun/moon icon and fires existing localStorage theme logic"
    - "Sidebar persists across navigations via transition:persist without stale active state"
    - "All nav controls have aria-current, aria-expanded, and focus-visible states"
  artifacts:
    - path: "astro.config.mjs"
      provides: "astro-icon integration registration"
      contains: "icon()"
    - path: "src/icons/home.svg"
      provides: "Home nav icon"
    - path: "src/icons/sun.svg"
      provides: "Custom sun icon for light mode toggle"
    - path: "src/icons/close.svg"
      provides: "Custom close/X icon for mobile drawer"
    - path: "src/layouts/BaseLayout.astro"
      provides: "Enhanced layout with ClientRouter, sidebar, skip-link, site-layout grid"
      contains: "ClientRouter"
    - path: "src/components/layout/Sidebar.astro"
      provides: "Desktop sidebar with nav, accordion, theme toggle"
      min_lines: 80
    - path: "src/components/ui/ThemeToggle.astro"
      provides: "Dark mode toggle button shared between sidebar and top bar"
      min_lines: 20
  key_links:
    - from: "src/layouts/BaseLayout.astro"
      to: "src/components/layout/Sidebar.astro"
      via: "import + transition:persist directive"
      pattern: "transition:persist"
    - from: "src/components/layout/Sidebar.astro"
      to: "src/components/ui/ThemeToggle.astro"
      via: "import and render in sidebar bottom section"
      pattern: "ThemeToggle"
    - from: "src/components/ui/ThemeToggle.astro"
      to: "localStorage theme"
      via: "astro:page-load event handler"
      pattern: "localStorage.*theme"
---

<objective>
Set up astro-icon integration, copy all needed pixelarticons SVGs (plus custom sun and close icons), enhance BaseLayout with ClientRouter and site-layout grid, build the desktop Sidebar with Obsidian-style accordion and dark mode toggle, and create placeholder pages for nav testing.

Purpose: Establishes the core navigation infrastructure that Plans 02 and 03 build on. After this plan, the desktop layout is fully functional with sidebar, accordion, theme toggle, and View Transitions persistence.

Output: Working desktop sidebar navigation with accordion, theme toggle, skip-to-content link, and ClientRouter-powered page transitions.
</objective>

<execution_context>
@C:\Users\mzbir\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\mzbir\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-navigation-shell/02-CONTEXT.md
@.planning/phases/02-navigation-shell/02-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs from Phase 1 codebase -->

From src/styles/tokens.css:
```css
--sidebar-width: 220px;
--bg-secondary: #F1F0EE;
--border-subtle: rgba(0, 0, 0, 0.06);
--accent: #1B7A3D;
--accent-hover: #145C2E;
--text-tertiary: #999999;
--z-base: 0;
--z-raised: 10;
--z-overlay: 100;
--z-modal: 200;
--space-2: 8px;
--space-3: 16px;
--space-4: 24px;
--font-mono: var(--font-jetbrains-mono), 'Courier New', monospace;
--size-xs: 11px;
--tracking-wide: 0.05em;
```

From src/styles/global.css:
```css
@layer reset, tokens, base, components, utilities;
/* .sr-only utility exists for skip link hidden state */
/* .grain-overlay at z-index: var(--z-overlay) */
```

From src/styles/typography.css:
```css
.mono-label {
  font-family: var(--font-mono);
  font-size: var(--size-xs);
  text-transform: uppercase;
  letter-spacing: var(--tracking-wide);
  color: var(--text-tertiary);
  font-weight: 400;
}
```

From src/layouts/BaseLayout.astro:
```astro
<!-- Current structure: html > head (Font, theme script) > body (grain-overlay, slot) -->
<!-- Theme script is:inline reads localStorage before first paint -->
<!-- astro:after-swap handler reapplies theme after View Transitions -->
```

From astro.config.mjs:
```js
import { defineConfig, fontProviders } from 'astro/config';
// Currently has: output: 'static', site, fonts array
// NO integrations array yet
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure astro-icon integration and copy all SVG icons</name>
  <files>
    astro.config.mjs,
    src/icons/home.svg,
    src/icons/briefcase.svg,
    src/icons/user.svg,
    src/icons/mail.svg,
    src/icons/moon.svg,
    src/icons/sun.svg,
    src/icons/cloud-sun.svg,
    src/icons/chevron-down.svg,
    src/icons/menu.svg,
    src/icons/close.svg,
    src/icons/download.svg,
    src/icons/link.svg,
    src/icons/map-pin.svg
  </files>
  <read_first>
    astro.config.mjs,
    node_modules/pixelarticons/svg/home.svg,
    node_modules/pixelarticons/svg/moon.svg
  </read_first>
  <action>
1. Add astro-icon as an Astro integration in `astro.config.mjs`:
   - Add `import icon from 'astro-icon';` at the top
   - Add `integrations: [icon()]` to the config object (after `site`, before `fonts`)

2. Create `src/icons/` directory.

3. Copy these SVGs from `node_modules/pixelarticons/svg/` to `src/icons/`:
   - home.svg, briefcase.svg, user.svg, mail.svg, moon.svg, cloud-sun.svg
   - chevron-down.svg, menu.svg, download.svg, link.svg, map-pin.svg
   All pixelarticons SVGs already use `fill="currentColor"`, 24x24 viewBox. Copy as-is.

4. Create a custom `src/icons/sun.svg` (pixelarticons has no sun icon). Create a 24x24 pixel-art-style sun using `fill="currentColor"`:
   ```svg
   <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="currentColor" viewBox="0 0 24 24">
     <path d="M11 1h2v4h-2V1ZM3.515 4.929l1.414-1.414L7.05 5.636 5.636 7.05 3.515 4.93ZM1 11h4v2H1v-2Zm2.515 8.071 2.12-2.121 1.415 1.414-2.121 2.121-1.414-1.414ZM11 19h2v4h-2v-4Zm5.536-1.05 1.414-1.414 2.121 2.121-1.414 1.414-2.121-2.121ZM19 11h4v2h-4v-2Zm-2.464-5.364 2.12-2.121 1.415 1.414-2.121 2.121-1.414-1.414ZM8 12a4 4 0 1 1 8 0 4 4 0 0 1-8 0Z"/>
   </svg>
   ```

5. Create a custom `src/icons/close.svg` (pixelarticons has no close/X icon). Create a 24x24 pixel-art-style X using `fill="currentColor"`:
   ```svg
   <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="currentColor" viewBox="0 0 24 24">
     <path d="M5 3H3v2h2v2h2v2h2v2H7v2H5v2H3v2h2v-2h2v-2h2v-2h2v2h2v2h2v2h2v-2h-2v-2h-2v-2h-2V9h2V7h2V5h2V3h-2v2h-2v2h-2v2h-2V7H9V5H7V3H5Z"/>
   </svg>
   ```

6. Verify the Icon component works by checking the build succeeds (Task 2 will use <Icon>).

Per D-06: Icons are pixelarticons via astro-icon. Per NAV-06: 24x24 SVG.
  </action>
  <verify>
    <automated>cd C:/Users/mzbir/Desktop/portfolio-website && ls src/icons/*.svg | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `astro.config.mjs` contains `import icon from 'astro-icon'`
    - `astro.config.mjs` contains `integrations: [icon()]`
    - `src/icons/` directory contains exactly 13 SVG files: home, briefcase, user, mail, moon, sun, cloud-sun, chevron-down, menu, close, download, link, map-pin
    - Every SVG has `fill="currentColor"` and `viewBox="0 0 24 24"`
    - sun.svg and close.svg are custom-created, not from pixelarticons
  </acceptance_criteria>
  <done>astro-icon integration configured, 13 SVG icons in src/icons/ (11 copied from pixelarticons, 2 custom), all using fill="currentColor" at 24x24</done>
</task>

<task type="auto">
  <name>Task 2: Enhance BaseLayout with ClientRouter, skip-to-content, site-layout grid, and placeholder pages</name>
  <files>
    src/layouts/BaseLayout.astro,
    src/components/layout/SkipLink.astro,
    src/pages/work/index.astro,
    src/pages/work/orbital-concierge.astro,
    src/pages/about.astro
  </files>
  <read_first>
    src/layouts/BaseLayout.astro,
    src/styles/global.css,
    src/styles/tokens.css,
    src/styles/layout.css,
    src/pages/index.astro
  </read_first>
  <action>
1. Create `src/components/layout/SkipLink.astro`:
   ```astro
   <a href="#main-content" class="skip-link">Skip to main content</a>
   <style>
     @layer components {
       .skip-link {
         position: absolute;
         top: var(--space-2);
         left: var(--space-2);
         z-index: var(--z-toast);
         background: var(--accent);
         color: #FFFFFF;
         padding: var(--space-2) var(--space-3);
         font-family: var(--font-mono);
         font-size: var(--size-sm);
         text-decoration: none;
         border-radius: 4px;
         transform: translateY(-200%);
         transition: transform 150ms ease;
       }
       .skip-link:focus {
         transform: translateY(0);
         outline: 2px solid var(--accent);
         outline-offset: 2px;
       }
     }
   </style>
   ```

2. Enhance `src/layouts/BaseLayout.astro`:
   - Add `import { ClientRouter } from 'astro:transitions';` in the frontmatter
   - Add `import SkipLink from '../components/layout/SkipLink.astro';` in the frontmatter
   - Add `const currentPath = Astro.url.pathname;` in the frontmatter
   - Place `<ClientRouter />` in `<head>` after the meta viewport tag, before the Font tags
   - Add `<SkipLink />` as the first child of `<body>`, before the grain overlay
   - Wrap the `<slot />` in a site-layout structure:
     ```html
     <div class="site-layout">
       <!-- Sidebar component will be added in Task 3 -->
       <main id="main-content" class="site-layout__main">
         <slot />
       </main>
       <!-- Footer component will be added in Plan 03 -->
     </div>
     ```
   - Add a `<style>` block with the site-layout CSS inside `@layer components`:
     ```css
     @layer components {
       .site-layout {
         display: grid;
         grid-template-columns: var(--sidebar-width) 1fr;
         grid-template-rows: 1fr auto;
         min-height: 100vh;
       }
       .site-layout__main {
         grid-column: 2;
         min-width: 0;
       }
       @media (max-width: 1199px) {
         .site-layout {
           grid-template-columns: 1fr;
           grid-template-rows: auto 1fr auto;
         }
         .site-layout__main {
           grid-column: 1;
         }
       }
     }
     ```
   - Keep the existing `is:inline` theme script and `astro:after-swap` handler UNTOUCHED (per D-15)

3. Create placeholder pages for nav testing:
   - `src/pages/work/index.astro`: Minimal page using BaseLayout with title "Work" and an `<h1>Work</h1>` placeholder
   - `src/pages/work/orbital-concierge.astro`: Minimal page using BaseLayout with title "Orbital Concierge" and an `<h1>Orbital Concierge</h1>` placeholder
   - `src/pages/about.astro`: Minimal page using BaseLayout with title "About" and an `<h1>About</h1>` placeholder
   Each page imports BaseLayout and wraps content in it. These are stubs for sidebar testing.

Per D-15: ClientRouter added. Per D-16: all scripts use astro:page-load. Per D-17: skip-to-content link added. Per LAY-01: BaseLayout wraps all pages with ClientRouter.
  </action>
  <verify>
    <automated>cd C:/Users/mzbir/Desktop/portfolio-website && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `src/layouts/BaseLayout.astro` contains `import { ClientRouter } from 'astro:transitions'`
    - `src/layouts/BaseLayout.astro` contains `<ClientRouter />`
    - `src/layouts/BaseLayout.astro` contains `id="main-content"` on the `<main>` element
    - `src/layouts/BaseLayout.astro` contains `class="site-layout"` wrapper div
    - `src/layouts/BaseLayout.astro` contains `grid-template-columns: var(--sidebar-width) 1fr`
    - `src/components/layout/SkipLink.astro` exists and links to `#main-content`
    - `src/pages/work/index.astro` exists with BaseLayout wrapper
    - `src/pages/work/orbital-concierge.astro` exists with BaseLayout wrapper
    - `src/pages/about.astro` exists with BaseLayout wrapper
    - `npm run build` completes with zero errors
    - Existing `is:inline` theme script and `astro:after-swap` handler are unchanged
  </acceptance_criteria>
  <done>BaseLayout enhanced with ClientRouter, skip-to-content link, and CSS grid site-layout. Three placeholder pages exist for nav testing. Build passes.</done>
</task>

<task type="auto">
  <name>Task 3: Build Sidebar component with accordion, ThemeToggle, ARIA, and View Transitions persistence</name>
  <files>
    src/components/layout/Sidebar.astro,
    src/components/ui/ThemeToggle.astro,
    src/layouts/BaseLayout.astro
  </files>
  <read_first>
    src/layouts/BaseLayout.astro,
    src/styles/tokens.css,
    src/styles/typography.css,
    .planning/phases/02-navigation-shell/02-RESEARCH.md
  </read_first>
  <action>
1. Create `src/components/ui/ThemeToggle.astro`:
   - A `<button>` element with `class="theme-toggle"`, `type="button"`, `aria-label="Toggle dark mode"`
   - Contains two `<Icon>` elements (import from `astro-icon/components`): `<Icon name="sun" size={24} />` and `<Icon name="moon" size={24} />`
   - Sun icon visible when `[data-theme="light"]`, moon icon visible when `[data-theme="dark"]`
   - Use CSS to show/hide: `[data-theme="light"] .theme-toggle__icon--dark { display: none; }` and vice versa
   - Add opacity crossfade transition (150ms ease) on the icon swap (per D-06 discretion)
   - Script block (NOT is:inline) that listens for click:
     ```js
     document.addEventListener('astro:page-load', () => {
       const toggle = document.querySelector('.theme-toggle');
       if (!toggle) return;
       toggle.addEventListener('click', () => {
         const current = document.documentElement.dataset.theme;
         const next = current === 'dark' ? 'light' : 'dark';
         document.documentElement.dataset.theme = next;
         localStorage.setItem('theme', next);
       });
     });
     ```
   - Style in `@layer components`. Button has `background: none`, `border: none`, `cursor: pointer`, `padding: var(--space-2)`, `color: var(--text-secondary)`, `border-radius: 4px`. Hover: `color: var(--text-primary)`, `background: var(--border-subtle)`.
   - Per D-07: fires same localStorage + data-theme logic. No new persistence mechanism.

2. Create `src/components/layout/Sidebar.astro`:
   - Accepts `currentPath` prop: `interface Props { currentPath: string; }`
   - Contains a `<nav>` element with `aria-label="Main navigation"` inside an `<aside>` with `class="sidebar"`
   - Define nav items array in frontmatter:
     ```js
     const navItems = [
       { label: 'Home', href: '/', icon: 'home' },
       { label: 'Work', href: '/work', icon: 'briefcase', children: [
         { label: 'Orbital Concierge', href: '/work/orbital-concierge' },
       ]},
       { label: 'About', href: '/about', icon: 'user' },
       { label: 'Contact', href: '/#contact', icon: 'mail' },
     ];
     ```
   - Render top-level items as `<ul class="sidebar__nav">` with `<li>` elements
   - For items WITHOUT children: render as `<a>` with `class="sidebar__link"`, `href`, `data-nav-link`, and `aria-current={currentPath === href ? 'page' : undefined}`
   - Each link contains `<Icon name={icon} size={24} />` and `<span class="sidebar__label mono-label">{label}</span>`
   - For items WITH children (Work): render a `<button class="nav-folder__trigger">` with `aria-expanded={currentPath.startsWith('/work/') ? 'true' : 'false'}` and `aria-controls="work-children"`
     - Button contains: Icon, label span, chevron-down Icon with `class="nav-folder__chevron"`
     - Below the button: `<div id="work-children" class="nav-folder__children" aria-hidden={currentPath.startsWith('/work/') ? 'false' : 'true'}>`
       - Inside: `<ul class="nav-folder__children-inner">` with child links
       - Each child: `<li><a href={child.href} class="nav-folder__child" data-nav-link aria-current={currentPath === child.href ? 'page' : undefined}>{child.label}</a></li>`
   - Also render the Work parent as a link: after the trigger button, include `<a href="/work" class="sidebar__link sidebar__link--parent" data-nav-link aria-current={currentPath === '/work' || currentPath === '/work/' ? 'page' : undefined}>` (this lets users click Work to go to the work index)
     Actually, simpler approach: make the trigger button wrap the icon+label and be the expand toggle. Add a separate link for Work index inside the children list at the top, OR make clicking "Work" text navigate while the chevron toggles. Per the Obsidian pattern, clicking the folder name navigates. Use this approach:
     - The "Work" item renders as an `<a>` link (navigates to /work) with the icon, PLUS a separate `<button>` for the chevron toggle
   - Bottom section: `<div class="sidebar__bottom">` containing the ThemeToggle component

   - Sidebar CSS (in `<style>` block, `@layer components`):
     - `.sidebar`: `position: sticky; top: 0; height: 100vh; overflow-y: auto; background: var(--bg-secondary); border-right: 1px solid var(--border-subtle); z-index: var(--z-raised); display: flex; flex-direction: column; padding: var(--space-4) 0;`
     - `.sidebar__nav`: `list-style: none; padding: 0; margin: 0; flex: 1;`
     - `.sidebar__link`: `display: flex; align-items: center; gap: var(--space-2); padding: var(--space-2) var(--space-4); color: var(--text-secondary); text-decoration: none; transition: color 150ms ease, background 150ms ease;`
     - `.sidebar__link:hover`: `color: var(--text-primary); background: var(--border-subtle);`
     - `.sidebar__link[aria-current="page"]`: `color: var(--accent); border-left: 3px solid var(--accent); padding-left: calc(var(--space-4) - 3px);` (active indicator per D-11)
     - `.sidebar__label`: inherits `.mono-label` styling (already uppercase, xs size, wide tracking, tertiary color). Override color to `inherit` so it follows the link color.
     - Nav folder accordion CSS:
       ```css
       .nav-folder__children {
         display: grid;
         grid-template-rows: 0fr;
         transition: grid-template-rows 250ms ease;
       }
       .nav-folder__children[aria-hidden="false"] {
         grid-template-rows: 1fr;
       }
       .nav-folder__children-inner {
         overflow: hidden;
         list-style: none;
         padding: 0;
         margin: 0;
       }
       .nav-folder__child {
         display: block;
         padding: var(--space-1) var(--space-4) var(--space-1) calc(var(--space-4) + 32px);
         font-family: var(--font-mono);
         font-size: 10px;
         text-transform: uppercase;
         letter-spacing: var(--tracking-wide);
         color: var(--text-tertiary);
         text-decoration: none;
         transition: color 150ms ease;
       }
       .nav-folder__child:hover { color: var(--text-primary); }
       .nav-folder__child[aria-current="page"] { color: var(--accent); }
       .nav-folder__chevron {
         margin-left: auto;
         transition: transform 250ms ease;
       }
       .nav-folder__children[aria-hidden="false"] ~ .nav-folder__trigger .nav-folder__chevron,
       .nav-folder__trigger[aria-expanded="true"] .nav-folder__chevron {
         transform: rotate(180deg);
       }
       ```
     - `.sidebar__bottom`: `padding: var(--space-3) var(--space-4); border-top: 1px solid var(--border-subtle);`
     - Hide sidebar below 1200px: `@media (max-width: 1199px) { .sidebar { display: none; } }`
     - Focus visible: `.sidebar__link:focus-visible, .nav-folder__trigger:focus-visible, .nav-folder__child:focus-visible { outline: 2px solid var(--accent); outline-offset: 2px; }`
     - `.nav-folder__trigger` button: `background: none; border: none; cursor: pointer; font: inherit; display: flex; align-items: center; gap: var(--space-2); padding: var(--space-2) var(--space-4); color: var(--text-secondary); width: 100%; transition: color 150ms ease, background 150ms ease;`
     - `.nav-folder__trigger:hover`: `color: var(--text-primary); background: var(--border-subtle);`

   - Script block for accordion toggle and route-aware state updates:
     ```js
     document.addEventListener('astro:page-load', () => {
       const currentPath = window.location.pathname;
       
       // Update aria-current on all nav links
       document.querySelectorAll('[data-nav-link]').forEach(link => {
         const href = link.getAttribute('href');
         const isActive = href === currentPath || href + '/' === currentPath || currentPath + '/' === href;
         if (isActive) {
           link.setAttribute('aria-current', 'page');
         } else {
           link.removeAttribute('aria-current');
         }
       });

       // Auto-expand Work folder if on a work subpage
       const workChildren = document.getElementById('work-children');
       const workTrigger = document.querySelector('[aria-controls="work-children"]');
       if (workChildren && workTrigger) {
         const shouldExpand = currentPath.startsWith('/work/');
         workChildren.setAttribute('aria-hidden', shouldExpand ? 'false' : 'true');
         workTrigger.setAttribute('aria-expanded', shouldExpand ? 'true' : 'false');
       }

       // Accordion toggle on click
       const triggers = document.querySelectorAll('.nav-folder__trigger');
       triggers.forEach(trigger => {
         trigger.addEventListener('click', () => {
           const targetId = trigger.getAttribute('aria-controls');
           const target = document.getElementById(targetId);
           if (!target) return;
           const isExpanded = trigger.getAttribute('aria-expanded') === 'true';
           trigger.setAttribute('aria-expanded', isExpanded ? 'false' : 'true');
           target.setAttribute('aria-hidden', isExpanded ? 'true' : 'false');
         });
       });

       // Close mobile drawer on navigation (pitfall prevention)
       const drawer = document.querySelector('.mobile-drawer');
       if (drawer && drawer.dataset.open === 'true') {
         drawer.dataset.open = 'false';
         document.body.style.overflow = '';
       }
     });
     ```

3. Wire Sidebar into BaseLayout:
   - Add `import Sidebar from '../components/layout/Sidebar.astro';` in BaseLayout frontmatter
   - Inside `<div class="site-layout">`, add `<Sidebar currentPath={currentPath} transition:persist />` as the first child, before `<main>`
   - The `transition:persist` directive keeps the sidebar DOM across navigations (per D-04)

Per D-01: Desktop sidebar at 220px. Per D-04: transition:persist. Per D-08/D-09: Obsidian folder accordion, one section at a time. Per D-10: CSS grid-template-rows transition. Per D-11: aria-current="page" + left border accent. Per NAV-05: aria-expanded, aria-current, focus-visible on all controls. Per D-16: all scripts use astro:page-load.
  </action>
  <verify>
    <automated>cd C:/Users/mzbir/Desktop/portfolio-website && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/layout/Sidebar.astro` exists and is at least 80 lines
    - `src/components/ui/ThemeToggle.astro` exists and is at least 20 lines
    - Sidebar.astro contains `aria-label="Main navigation"`
    - Sidebar.astro contains `aria-expanded` on the Work folder trigger
    - Sidebar.astro contains `aria-controls="work-children"`
    - Sidebar.astro contains `aria-current` attribute logic
    - Sidebar.astro contains `grid-template-rows: 0fr` for accordion
    - Sidebar.astro contains `data-nav-link` on all nav links
    - Sidebar.astro contains `astro:page-load` event listener
    - Sidebar.astro contains `:focus-visible` styles
    - ThemeToggle.astro contains `localStorage.setItem('theme'`
    - ThemeToggle.astro contains `document.documentElement.dataset.theme`
    - BaseLayout.astro contains `transition:persist` on the Sidebar component
    - BaseLayout.astro contains `import Sidebar`
    - `npm run build` completes with zero errors
  </acceptance_criteria>
  <done>Desktop sidebar renders with 4 nav items (Home, Work, About, Contact) using pixelarticons. Work folder accordion expands/collapses with CSS grid-template-rows transition. Dark mode toggle swaps theme. Active page highlighted with green left border. Sidebar persists across View Transition navigations with correct active state updates. All controls are keyboard accessible with focus-visible outlines.</done>
</task>

</tasks>

<verification>
- `npm run build` passes with zero errors
- `src/icons/` contains 13 SVG files
- `astro.config.mjs` has `icon()` in integrations
- BaseLayout has ClientRouter, site-layout grid, skip-link, sidebar with transition:persist
- Sidebar has 4 nav items with pixelarticons, Work accordion, ThemeToggle, ARIA attributes
- All scripts use `astro:page-load`, none use `DOMContentLoaded`
</verification>

<success_criteria>
Desktop sidebar navigation is fully functional: nav items with icons, Obsidian-style folder accordion on Work section, dark mode toggle, skip-to-content link, View Transitions via ClientRouter, and sidebar persists across navigations. Build passes. Placeholder pages exist at /work, /work/orbital-concierge, and /about for testing.
</success_criteria>

<output>
After completion, create `.planning/phases/02-navigation-shell/02-01-SUMMARY.md`
</output>
