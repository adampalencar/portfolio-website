---
phase: 02-navigation-shell
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified: []
autonomous: false
requirements:
  - NAV-01
  - NAV-02
  - NAV-03
  - NAV-04
  - NAV-05
  - NAV-06
  - LAY-01
  - LAY-02
  - LAY-03

must_haves:
  truths:
    - "Desktop sidebar renders at 220px with pixelarticons and accordion"
    - "Tablet top bar shows inline nav items"
    - "Mobile hamburger opens slide-in drawer"
    - "Dark mode toggle works in all three breakpoints"
    - "Footer displays all 5 items correctly"
    - "Skip-to-content link works"
    - "Page navigations use View Transitions without sidebar flicker"
  artifacts: []
  key_links: []
---

<objective>
Visual verification checkpoint at all three breakpoints. Confirm the entire navigation shell works: sidebar, top bar, mobile drawer, accordion, dark mode toggle, footer, skip-to-content, and View Transitions.

Purpose: Catches visual or interaction issues before Phase 3 builds content on top of this shell. Human eyes verify what automated builds cannot.

Output: User approval or issue list for correction.
</objective>

<execution_context>
@C:\Users\mzbir\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\mzbir\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/phases/02-navigation-shell/02-CONTEXT.md
@.planning/phases/02-navigation-shell/02-01-SUMMARY.md
@.planning/phases/02-navigation-shell/02-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Visual verification of navigation shell at all breakpoints</name>
  <files>src/layouts/BaseLayout.astro</files>
  <action>
    Start the dev server with `npm run dev` and present the 20-point verification checklist below to the user. Run `npm run build` first to confirm the build passes, then start the dev server.

    **What was built:** Complete navigation shell: desktop sidebar with Obsidian-style folder accordion, tablet top bar, mobile hamburger drawer, dark mode toggle, dense footer, skip-to-content link, and View Transitions persistence. Built across Plans 01 and 02.

    **Verification checklist for the user:**

    **Desktop (1200px+ viewport):**
    1. Sidebar visible at left, 220px wide, with Home/Work/About/Contact nav items using pixel art icons
    2. Click "Work" chevron: accordion expands to show "Orbital Concierge" child link
    3. Click a nav link: page changes via View Transition, sidebar does NOT flicker or re-render
    4. On /work/orbital-concierge: Work folder is auto-expanded, child link highlighted green
    5. Dark mode toggle in sidebar bottom: click to swap theme, icon changes sun/moon
    6. Footer visible below content: email, LinkedIn, resume, location, v0.1 on single row
    7. Tab through all nav controls: each gets a visible green outline (focus-visible)
    8. Hit Tab from the very top of the page: skip-to-content link appears, activating it jumps to main content

    **Tablet (resize to ~900px):**
    9. Sidebar disappears, replaced by horizontal top bar with nav items inline
    10. Dark mode toggle visible in top bar
    11. Footer stacks or wraps appropriately

    **Mobile (resize to ~375px):**
    12. Top bar shows hamburger icon only (inline nav hidden)
    13. Click hamburger: drawer slides in from left with semi-transparent backdrop
    14. Drawer has same nav items, Work accordion works
    15. Click backdrop or X button: drawer closes
    16. Press Escape: drawer closes
    17. While drawer is open: page behind does not scroll
    18. Footer items stack vertically, pipe separators hidden

    **Dark mode persistence:**
    19. Toggle dark mode, refresh page: theme persists (no flash)
    20. Toggle dark mode, navigate to another page: theme persists
  </action>
  <verify>
    <automated>cd C:/Users/mzbir/Desktop/portfolio-website && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` passes with zero errors
    - User confirms all 20 verification points pass
    - OR user lists specific issues to fix (triggers gap closure)
  </acceptance_criteria>
  <done>User confirms the navigation shell looks and behaves correctly at all three breakpoints. Phase 2 is complete and ready for Phase 3 content infrastructure.</done>
</task>

</tasks>

<verification>
Human visual verification of all Phase 2 requirements at all three breakpoints.
</verification>

<success_criteria>
User confirms the navigation shell looks and behaves correctly at desktop (1200px+), tablet (768-1199px), and mobile (<768px). All interactive controls work: accordion, theme toggle, hamburger drawer, keyboard navigation, skip link. No visual regressions from Phase 1 (grain texture, typography, colors intact).
</success_criteria>

<output>
After completion, create `.planning/phases/02-navigation-shell/02-03-SUMMARY.md`
</output>
